-- Drops the database.
DROP DATABASE IF EXISTS GenericCompany;
-- Drops users for this database only.
DROP USER IF EXISTS 'GenericAdministrator'@'localhost';
DROP USER IF EXISTS 'GenericApplication'@'localhost';
